// Object Data Type

// In JS, an object is a collection of properties.

// where each property is define as a key-value pair.

let person = {
  name: "John",
  age: 30,
  city: "New York"
};

console.log(person);
console.log(typeof person);